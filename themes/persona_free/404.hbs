{{> head}}
{{> navbar}}
     <main class="main page page--error">
        <div class="hero hero--centered">
            <h1 class="page--error__title">
                {{ translate 'error.404' }}
            </h1>                
            <p>{{ translate 'error.message' }}</p>
            <p class="page--error__btn">
               <a href="{{@website.url}}" class="btn">{{ translate 'error.button' }}</a>
            </p>
        </div>
    </main>
{{> footer}}
